apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - pdb.yaml

labels:
  - pairs:
      app.kubernetes.io/name: hello-api
      app.kubernetes.io/part-of: devops-assignment
    includeSelectors: true
    includeTemplates: true
